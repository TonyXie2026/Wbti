{
  "name": "internet-persona-test",
  "version": "1.0.0",
  "private": true,
  "description": "Static WBTI site. npm run dev is for local preview only; static hosting platforms can deploy the repository directly.",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node server.mjs",
    "check": "node --check server.mjs && node --check src/main.js && node --check src/logic/quiz.js && node --check src/ui/templates.js"
  }
}
